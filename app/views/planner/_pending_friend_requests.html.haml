- if current_user.friend_requests.any?
  %h4
    Awaiting Approval
    %span.badge #{current_user.friend_requests.count}

  - for request in current_user.friend_requests
    .friend-panel-info
      == #{link_to(request.user.name, user_path(request.user))} wants to be your #{request.type}
      .friend-btns
        = link_to("Approve", friendship_path(request, friendship: {confirmed: true}), method: :patch, class: "btn btn-success btn-xs")
        = link_to("Ignore", friendship_path(request), method: :delete, class: "btn btn-danger btn-xs")